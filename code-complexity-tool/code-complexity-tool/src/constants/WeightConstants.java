/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package constants;

/**
 *
 * @author Laksara Welideniya
 */
public class WeightConstants {
    
    //Reference (&) and dereference (*) operators
    public final static int REFDEREF = 2;
    
    //‘new’, ‘delete’, ‘throw’, and ‘throws’ key words
    public final static int NDTTKEYWORD = 2;
    
    //Arithmetic operators → { + - * / % ++ -- }
    public final static int ARITHMETICOPS = 1;
    
    //Relation operators → { == != > < >= <= }
    public final static int RELATIONALOPS = 1;
    
    //Logical operators → { && || ! }
    public final static int LOGICALOPS = 1;
    
    //Bitwise operators → { | ^ ~ << >> >>> <<< }
    public final static int BITWISEOPS = 1;
    
    //Miscellaneous operators → { , ->. :: }
    public final static int MISCELLANEOUSOPS = 1;
    
    //Assignment operators → {+= -= *= /= = >>>= |= &= %= <<= >>= ^= }
    public final static int ASSIGNMENTOPS = 1;
    
    //Key words → {void, double, int, float, string, printf, println, cout, cin, ‘if’, ‘for’, ‘while’, ‘do-while’, ‘switch’, ‘case’ etc.}
    public final static int KEYWORDOPS = 1;
    
    //Manipulators → {‘endl’, ‘ \n’, etc. }
    public final static int MANIPULATORS = 1;
    
    //Text inside a pair of double quotes → {Eg: “The greatest is” }
    public final static int QUOTES = 1;
    
    //Class, method, object, variable, and array names
    public final static int NAMES = 1;
    
    //Numeric values (numbers)
    public final static int NUMBERS = 1;  
    
}
