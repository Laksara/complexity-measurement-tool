/*
* To change this license header, choose License Headers in Project Properties.
* To change this template file, choose Tools | Templates
* and open the template in the editor.
*/
package constants;

/**
 *
 * @author Laksara Welideniya
 */
public class WeightConstants {
    
    //Reference (&) and dereference (*) operators
    public final static double REFDEREF = 2.0;
    
    //‘new’, ‘delete’, ‘throw’, and ‘throws’ key words
    public final static double NDTTKEYWORD = 2.0;
    
    //Arithmetic operators → { + - * / % ++ -- }
    public final static double ARITHMETICOPS = 1.0;
    
    //Relation operators → { == != > < >= <= }
    public final static double RELATIONALOPS = 1.0;
    
    //Logical operators → { && || ! }
    public final static double LOGICALOPS = 1.0;
    
    //Bitwise operators → { | ^ ~ << >> >>> <<< }
    public final static double BITWISEOPS = 1.0;
    
    //Miscellaneous operators → { , ->. :: }
    public final static double MISCELLANEOUSOPS = 1.0;
    
    //Assignment operators → {+= -= *= /= = >>>= |= &= %= <<= >>= ^= }
    public final static double ASSIGNMENTOPS = 1.0;
    
    //Key words → {void, double, int, float, string, printf, println, cout, cin, ‘if’, ‘for’, ‘while’, ‘do-while’, ‘switch’, ‘case’ etc.}
    public final static double KEYWORDOPS = 1.0;
    
    //Manipulators → {‘endl’, ‘ \n’, etc. }
    public final static double MANIPULATORS = 1.0;
    
    //Text inside a pair of double quotes → {Eg: “The greatest is” }
    public final static double QUOTES = 0.5;
    
    //Class, method, object, variable, and array names
    public final static double NAMES = 1.0;
    
    //Numeric values (numbers)
    public final static double NUMBERS = 1.0;
    
    //inheritance values
    public final static double INHERITANCE = 1.0;
}
